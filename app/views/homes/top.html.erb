<div class="container">
  <h1>トップページ</h1>
  <p>トラベルマップ</p>
  <p>※日本国内の旅行を想定しています。</p>
  <div class="top_post">
    <h3><i class="fas fa-plane-departure"></i>みんなの旅行スポット</h3>
    <% @random_posts.each do |post| %>
      <%= link_to post_path(post.id) do %>
        <%= image_tag attachment_url(post, :image, :fill, 200, 200, format: "jpg"), class: "img" %>
      <% end %>
    <% end %>
  </div>
  <br>
  <div class="top_post">
    <h3>new! 新着旅行スポット</h3>
    <% @new_posts.each do |post| %>
      <%= link_to post_path(post.id) do %>
        <%= image_tag attachment_url(post, :image, :fill, 200, 200, format: "jpg"), class: "img" %>
      <% end %>
    <% end %>
  </div>
</div>